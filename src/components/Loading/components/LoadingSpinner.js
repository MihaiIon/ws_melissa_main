import React, { useState, useContext } from "react";
import PropTypes from "prop-types";

function LoadingSpinner(props) {
  return <div className="c-loading_spinner" />;
}

LoadingSpinner.propTypes = {};

export default LoadingSpinner;
